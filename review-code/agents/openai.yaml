interface:
  display_name: "Review Code"
  short_description: "Orchestrate risk-routed, multi-checklist code review"
  default_prompt: "Use $review-code to review the current diff: risk-route to the relevant review checklists, run each as a subagent over its references/<topic>-checklist.md plus references/shared-rubric.md, verify important findings, and return one consolidated severity-ranked review."
