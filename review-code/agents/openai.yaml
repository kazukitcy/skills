interface:
  display_name: "Review Code"
  short_description: "Orchestrate risk-routed, multi-specialist code review"
  default_prompt: "Use $review-code to review the current diff: risk-route to relevant review-code-* specialists, run them as subagents, verify important findings, and return one consolidated severity-ranked review."
